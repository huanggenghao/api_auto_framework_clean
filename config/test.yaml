http:
  timeout: 30
  verify_ssl: false
  # 相对路径请求会与 base_url 拼接；留空则仅用 YAML 中的绝对 URL
  base_url: ""
  default_headers: {}
  retry:
    total: 3
    backoff_seconds: 0.5
    retry_status_codes: [502, 503, 504]

auth:
  login_url: "https://dpt-cn-dev.ugreeniot.com/app/user/login"
  login_account: "15992213991"
  login_password: "abcd1234"
  merchant_id: "100000000000000000"
  phone_code: "86"

# 报告邮件（敏感信息建议用环境变量覆盖，见 run.py）
email:
  smtp_server: "smtp.163.com"
  port: 465
  sender: ""
  psw: ""
  receiver: ""
